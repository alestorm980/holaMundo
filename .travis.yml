language: cpp
compiler:
  - g++
before_install:
  -sudo apt-get install -y libgtest-dev
script:
  - cd Analisis
  - mkdir build
  - cd helloworld
  - cmake cmake -H. -B../build
  - cd ../build
  - make
  - ./hello
  - ls
  - ls

  
