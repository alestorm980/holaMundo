language: cpp
compiler:
  - g++
script:
  - sudo apt-get install libgtest0 libgtest-dev
  - cd Analisis
  - mkdir build
  - cd helloworld
  - cmake cmake -H. -B../build
  - cd ../build
  - make
  - ./hello
  - ls

  
