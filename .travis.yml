language: cpp
compiler:
  - g++
script:
  -sudo apt-get install libgtest-dev
  - cd Analisis
  - mkdir build
  - cd helloworld
  - cmake cmake -H. -B../build
  - cd ../build
  - make
  - ./hello
  - ls
  - ls

  
